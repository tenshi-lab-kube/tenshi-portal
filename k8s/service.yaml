apiVersion: v1
kind: Service
metadata:
  name: tenshi-portal
  namespace: tenshi-portal
  labels:
    app.kubernetes.io/name: tenshi-portal
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tenshi-portal-auth
  ports:
    - name: http
      port: 8080
      targetPort: http
